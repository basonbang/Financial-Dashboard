// ❕ This is how you create new pages to go to in your app: Create a new folder that defines a route -> Add page file inside of it
// Only content WITHIN "page" is publicly accessible

export default function Page() {
    return <p>Dashboard Page</p>;
  }